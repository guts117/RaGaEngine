cmake_minimum_required(VERSION 3.10)

project(NarakaKar VERSION 0.1)

add_subdirectory(PhysicsEngine)
add_subdirectory(NarakaKarEngine RenderEngine) # TODO(mdizdar): the render engine folder is scheduled to be renamed at some point